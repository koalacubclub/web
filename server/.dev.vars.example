# Copy to `.dev.vars` for local `wrangler dev`. `.dev.vars` is gitignored.
# In production set this with: wrangler secret put SESSION_SECRET --env production
SESSION_SECRET="local-dev-secret-change-me"
